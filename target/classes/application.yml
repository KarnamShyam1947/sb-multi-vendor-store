server:
    address: 0.0.0.0
    port: 8080

spring:
    application:
        name: ecommerce

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: admin
        url: jdbc:mysql://localhost:3306/ecommerce
        username: admin

    freemarker:
        suffix: .ftl
        template-loader-path: classpath:/templates/email/

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

    mail:
        host: smtp.gmail.com
        password: gegelxonrrxarddq
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
        protocol: smtp
        username: vitap.library@gmail.com


                        

